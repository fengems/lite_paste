## 交互

- 始终使用中文，回复开头称“老板”，结尾写 `置信度：0-100%`。
- 信息不足或存在多种合理解释时，先确认，不猜测。
- 仅在用户明确要求时提交代码；“发版”视为允许按发版流程提交、打 tag、推送。

## 提交与发版

- 提交前检查改动范围，不混入无关变更。
- 提交信息前缀：`feat`、`fix`、`hotfix`、`docs`、`style`、`refactor`、`test`、`chore`。
- 提交信息主体：简要描述改动，必要时补充背景、原因和影响；不写实现细节、过程或提示词式语言；使用中文。
- 发版顺序：提交业务代码；code review 并修复；运行必要测试；若 CR/test 产生修复，先提交修复；更新版本文件；单独提交版本改动；创建版本 tag；推送提交和 tag。
- 未指定版本号时，仅递增补丁版本（patch）；tag 名与版本号一致。
- 常见版本文件包括 `package.json`、`pyproject.toml` 等。

## 文件与安全

- 不在根目录创建测试、临时或多余 Markdown 文件。
- 测试放测试目录，文档放 `docs`，临时文件放明确临时目录。
- 功能、方案或行为有重要变化时更新 `docs`；无对应文档先确认是否新增。
- 记录日期必须用 `date` 等命令获取真实日期。
- 不提交密钥、令牌、私有配置或无关生成物；发现敏感信息先提醒。

## 工程方法

- 编码前明确需求、边界和接口；不明确先问。
- 先设计数据结构、错误模型和不变量，再写流程代码。
- 保持概念完整：同层抽象使用一致命名、返回值和错误约定。
- 复杂分支优先用表驱动、策略映射、状态机或清晰数据结构消除。
- 小步修改，保持可回滚；新增依赖前确认必要性，优先标准库和既有依赖。
- 优先从 `README`、构建文件或 CI 配置查找命令；不要编造测试、构建或发版命令。

## 代码质量

- 代码写给人看，遵循 KISS、DRY，避免炫技、过度设计和无意义抽象。
- 优先使用项目既有风格、工具链和格式化规则。
- 模块化实现：核心逻辑、IO、副作用分离，函数只做一件事。
- 函数逻辑代码优先不超过 40 行，超过 60 行必须拆分或说明理由。
- 单文件优先不超过 400 行，超过 600 行必须拆分模块或说明理由。
- 避免深层嵌套、隐式副作用、魔法值、重复分支和含糊缩写。
- 错误处理保持清晰、可诊断；用校验、断言或类型约束保护不变量。
- 注释解释意图和背景，不复述代码过程。
- 界面文案面向最终用户，不写开发者沟通、实现说明或提示词式语言。
- 新增或修复关键逻辑时补充最小必要测试；改动后运行相关检查，无法运行时说明原因。
